// File generated from our OpenAPI spec by Stainless.

package com.openai.models.beta.responses

import com.fasterxml.jackson.module.kotlin.jacksonTypeRef
import com.openai.core.JsonValue
import com.openai.core.jsonMapper
import com.openai.errors.OpenAIInvalidDataException
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.assertThrows
import org.junit.jupiter.params.ParameterizedTest
import org.junit.jupiter.params.provider.EnumSource

internal class BetaResponseStreamEventTest {

    @Test
    fun ofResponseAudioDelta() {
        val responseAudioDelta =
            BetaResponseAudioDeltaEvent.builder()
                .delta("delta")
                .sequenceNumber(0L)
                .agent(BetaResponseAudioDeltaEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseAudioDelta(responseAudioDelta)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).contains(responseAudioDelta)
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseAudioDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseAudioDelta(
                BetaResponseAudioDeltaEvent.builder()
                    .delta("delta")
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseAudioDeltaEvent.Agent.builder().agentName("agent_name").build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseAudioDone() {
        val responseAudioDone =
            BetaResponseAudioDoneEvent.builder()
                .sequenceNumber(0L)
                .agent(BetaResponseAudioDoneEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent = BetaResponseStreamEvent.ofResponseAudioDone(responseAudioDone)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).contains(responseAudioDone)
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseAudioDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseAudioDone(
                BetaResponseAudioDoneEvent.builder()
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseAudioDoneEvent.Agent.builder().agentName("agent_name").build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseAudioTranscriptDelta() {
        val responseAudioTranscriptDelta =
            BetaResponseAudioTranscriptDeltaEvent.builder()
                .delta("delta")
                .sequenceNumber(0L)
                .agent(
                    BetaResponseAudioTranscriptDeltaEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseAudioTranscriptDelta(responseAudioTranscriptDelta)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta())
            .contains(responseAudioTranscriptDelta)
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseAudioTranscriptDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseAudioTranscriptDelta(
                BetaResponseAudioTranscriptDeltaEvent.builder()
                    .delta("delta")
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseAudioTranscriptDeltaEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseAudioTranscriptDone() {
        val responseAudioTranscriptDone =
            BetaResponseAudioTranscriptDoneEvent.builder()
                .sequenceNumber(0L)
                .agent(
                    BetaResponseAudioTranscriptDoneEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseAudioTranscriptDone(responseAudioTranscriptDone)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone())
            .contains(responseAudioTranscriptDone)
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseAudioTranscriptDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseAudioTranscriptDone(
                BetaResponseAudioTranscriptDoneEvent.builder()
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseAudioTranscriptDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCodeInterpreterCallCodeDelta() {
        val responseCodeInterpreterCallCodeDelta =
            BetaResponseCodeInterpreterCallCodeDeltaEvent.builder()
                .delta("delta")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseCodeInterpreterCallCodeDeltaEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallCodeDelta(
                responseCodeInterpreterCallCodeDelta
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta())
            .contains(responseCodeInterpreterCallCodeDelta)
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseCodeInterpreterCallCodeDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallCodeDelta(
                BetaResponseCodeInterpreterCallCodeDeltaEvent.builder()
                    .delta("delta")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseCodeInterpreterCallCodeDeltaEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCodeInterpreterCallCodeDone() {
        val responseCodeInterpreterCallCodeDone =
            BetaResponseCodeInterpreterCallCodeDoneEvent.builder()
                .code("code")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseCodeInterpreterCallCodeDoneEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallCodeDone(
                responseCodeInterpreterCallCodeDone
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone())
            .contains(responseCodeInterpreterCallCodeDone)
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseCodeInterpreterCallCodeDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallCodeDone(
                BetaResponseCodeInterpreterCallCodeDoneEvent.builder()
                    .code("code")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseCodeInterpreterCallCodeDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCodeInterpreterCallCompleted() {
        val responseCodeInterpreterCallCompleted =
            BetaResponseCodeInterpreterCallCompletedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseCodeInterpreterCallCompletedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallCompleted(
                responseCodeInterpreterCallCompleted
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted())
            .contains(responseCodeInterpreterCallCompleted)
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseCodeInterpreterCallCompletedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallCompleted(
                BetaResponseCodeInterpreterCallCompletedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseCodeInterpreterCallCompletedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCodeInterpreterCallInProgress() {
        val responseCodeInterpreterCallInProgress =
            BetaResponseCodeInterpreterCallInProgressEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseCodeInterpreterCallInProgressEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallInProgress(
                responseCodeInterpreterCallInProgress
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress())
            .contains(responseCodeInterpreterCallInProgress)
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseCodeInterpreterCallInProgressRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallInProgress(
                BetaResponseCodeInterpreterCallInProgressEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseCodeInterpreterCallInProgressEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCodeInterpreterCallInterpreting() {
        val responseCodeInterpreterCallInterpreting =
            BetaResponseCodeInterpreterCallInterpretingEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseCodeInterpreterCallInterpretingEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallInterpreting(
                responseCodeInterpreterCallInterpreting
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting())
            .contains(responseCodeInterpreterCallInterpreting)
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseCodeInterpreterCallInterpretingRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCodeInterpreterCallInterpreting(
                BetaResponseCodeInterpreterCallInterpretingEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseCodeInterpreterCallInterpretingEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCompleted() {
        val responseCompleted =
            BetaResponseCompletedEvent.builder()
                .response(
                    BetaResponse.builder()
                        .id("id")
                        .createdAt(0.0)
                        .error(
                            BetaResponseError.builder()
                                .code(BetaResponseError.Code.SERVER_ERROR)
                                .message("message")
                                .build()
                        )
                        .incompleteDetails(
                            BetaResponse.IncompleteDetails.builder()
                                .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                .build()
                        )
                        .instructions("string")
                        .metadata(
                            BetaResponse.Metadata.builder()
                                .putAdditionalProperty("foo", JsonValue.from("string"))
                                .build()
                        )
                        .model(BetaResponse.Model.GPT_5_1)
                        .addOutput(
                            BetaResponseOutputMessage.builder()
                                .id("id")
                                .addContent(
                                    BetaResponseOutputText.builder()
                                        .addAnnotation(
                                            BetaResponseOutputText.Annotation.FileCitation.builder()
                                                .fileId("file_id")
                                                .filename("filename")
                                                .index(0L)
                                                .build()
                                        )
                                        .text("text")
                                        .addLogprob(
                                            BetaResponseOutputText.Logprob.builder()
                                                .token("token")
                                                .addByte(0L)
                                                .logprob(0.0)
                                                .addTopLogprob(
                                                    BetaResponseOutputText.Logprob.TopLogprob
                                                        .builder()
                                                        .token("token")
                                                        .addByte(0L)
                                                        .logprob(0.0)
                                                        .build()
                                                )
                                                .build()
                                        )
                                        .build()
                                )
                                .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                .agent(
                                    BetaResponseOutputMessage.Agent.builder()
                                        .agentName("agent_name")
                                        .build()
                                )
                                .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                .build()
                        )
                        .parallelToolCalls(true)
                        .temperature(1.0)
                        .toolChoice(BetaToolChoiceOptions.NONE)
                        .addTool(
                            BetaFunctionTool.builder()
                                .name("name")
                                .parameters(
                                    BetaFunctionTool.Parameters.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .strict(true)
                                .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                .deferLoading(true)
                                .description("description")
                                .outputSchema(
                                    BetaFunctionTool.OutputSchema.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .build()
                        )
                        .topP(1.0)
                        .background(true)
                        .completedAt(0.0)
                        .conversation(BetaResponse.Conversation.builder().id("id").build())
                        .maxOutputTokens(0L)
                        .maxToolCalls(0L)
                        .moderation(
                            BetaResponse.Moderation.builder()
                                .input(
                                    BetaResponse.Moderation.Input.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .output(
                                    BetaResponse.Moderation.Output.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .build()
                        )
                        .previousResponseId("previous_response_id")
                        .prompt(
                            BetaResponsePrompt.builder()
                                .id("id")
                                .variables(
                                    BetaResponsePrompt.Variables.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("string"))
                                        .build()
                                )
                                .version("version")
                                .build()
                        )
                        .promptCacheKey("prompt-cache-key-1234")
                        .promptCacheOptions(
                            BetaResponse.PromptCacheOptions.builder()
                                .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                .build()
                        )
                        .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                        .reasoning(
                            BetaResponse.Reasoning.builder()
                                .context(BetaResponse.Reasoning.Context.AUTO)
                                .effort(BetaResponse.Reasoning.Effort.NONE)
                                .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                .summary(BetaResponse.Reasoning.Summary.AUTO)
                                .build()
                        )
                        .safetyIdentifier("safety-identifier-1234")
                        .serviceTier(BetaResponse.ServiceTier.AUTO)
                        .status(BetaResponseStatus.COMPLETED)
                        .text(
                            BetaResponseTextConfig.builder()
                                .formatText()
                                .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                .build()
                        )
                        .topLogprobs(0L)
                        .truncation(BetaResponse.Truncation.AUTO)
                        .usage(
                            BetaResponseUsage.builder()
                                .inputTokens(0L)
                                .inputTokensDetails(
                                    BetaResponseUsage.InputTokensDetails.builder()
                                        .cacheWriteTokens(0L)
                                        .cachedTokens(0L)
                                        .build()
                                )
                                .outputTokens(0L)
                                .outputTokensDetails(
                                    BetaResponseUsage.OutputTokensDetails.builder()
                                        .reasoningTokens(0L)
                                        .build()
                                )
                                .totalTokens(0L)
                                .build()
                        )
                        .user("user-1234")
                        .build()
                )
                .sequenceNumber(0L)
                .agent(BetaResponseCompletedEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent = BetaResponseStreamEvent.ofResponseCompleted(responseCompleted)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).contains(responseCompleted)
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseCompletedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCompleted(
                BetaResponseCompletedEvent.builder()
                    .response(
                        BetaResponse.builder()
                            .id("id")
                            .createdAt(0.0)
                            .error(
                                BetaResponseError.builder()
                                    .code(BetaResponseError.Code.SERVER_ERROR)
                                    .message("message")
                                    .build()
                            )
                            .incompleteDetails(
                                BetaResponse.IncompleteDetails.builder()
                                    .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                    .build()
                            )
                            .instructions("string")
                            .metadata(
                                BetaResponse.Metadata.builder()
                                    .putAdditionalProperty("foo", JsonValue.from("string"))
                                    .build()
                            )
                            .model(BetaResponse.Model.GPT_5_1)
                            .addOutput(
                                BetaResponseOutputMessage.builder()
                                    .id("id")
                                    .addContent(
                                        BetaResponseOutputText.builder()
                                            .addAnnotation(
                                                BetaResponseOutputText.Annotation.FileCitation
                                                    .builder()
                                                    .fileId("file_id")
                                                    .filename("filename")
                                                    .index(0L)
                                                    .build()
                                            )
                                            .text("text")
                                            .addLogprob(
                                                BetaResponseOutputText.Logprob.builder()
                                                    .token("token")
                                                    .addByte(0L)
                                                    .logprob(0.0)
                                                    .addTopLogprob(
                                                        BetaResponseOutputText.Logprob.TopLogprob
                                                            .builder()
                                                            .token("token")
                                                            .addByte(0L)
                                                            .logprob(0.0)
                                                            .build()
                                                    )
                                                    .build()
                                            )
                                            .build()
                                    )
                                    .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                    .agent(
                                        BetaResponseOutputMessage.Agent.builder()
                                            .agentName("agent_name")
                                            .build()
                                    )
                                    .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                    .build()
                            )
                            .parallelToolCalls(true)
                            .temperature(1.0)
                            .toolChoice(BetaToolChoiceOptions.NONE)
                            .addTool(
                                BetaFunctionTool.builder()
                                    .name("name")
                                    .parameters(
                                        BetaFunctionTool.Parameters.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .strict(true)
                                    .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                    .deferLoading(true)
                                    .description("description")
                                    .outputSchema(
                                        BetaFunctionTool.OutputSchema.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .build()
                            )
                            .topP(1.0)
                            .background(true)
                            .completedAt(0.0)
                            .conversation(BetaResponse.Conversation.builder().id("id").build())
                            .maxOutputTokens(0L)
                            .maxToolCalls(0L)
                            .moderation(
                                BetaResponse.Moderation.builder()
                                    .input(
                                        BetaResponse.Moderation.Input.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .output(
                                        BetaResponse.Moderation.Output.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .build()
                            )
                            .previousResponseId("previous_response_id")
                            .prompt(
                                BetaResponsePrompt.builder()
                                    .id("id")
                                    .variables(
                                        BetaResponsePrompt.Variables.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("string"))
                                            .build()
                                    )
                                    .version("version")
                                    .build()
                            )
                            .promptCacheKey("prompt-cache-key-1234")
                            .promptCacheOptions(
                                BetaResponse.PromptCacheOptions.builder()
                                    .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                    .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                    .build()
                            )
                            .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                            .reasoning(
                                BetaResponse.Reasoning.builder()
                                    .context(BetaResponse.Reasoning.Context.AUTO)
                                    .effort(BetaResponse.Reasoning.Effort.NONE)
                                    .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                    .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                    .summary(BetaResponse.Reasoning.Summary.AUTO)
                                    .build()
                            )
                            .safetyIdentifier("safety-identifier-1234")
                            .serviceTier(BetaResponse.ServiceTier.AUTO)
                            .status(BetaResponseStatus.COMPLETED)
                            .text(
                                BetaResponseTextConfig.builder()
                                    .formatText()
                                    .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                    .build()
                            )
                            .topLogprobs(0L)
                            .truncation(BetaResponse.Truncation.AUTO)
                            .usage(
                                BetaResponseUsage.builder()
                                    .inputTokens(0L)
                                    .inputTokensDetails(
                                        BetaResponseUsage.InputTokensDetails.builder()
                                            .cacheWriteTokens(0L)
                                            .cachedTokens(0L)
                                            .build()
                                    )
                                    .outputTokens(0L)
                                    .outputTokensDetails(
                                        BetaResponseUsage.OutputTokensDetails.builder()
                                            .reasoningTokens(0L)
                                            .build()
                                    )
                                    .totalTokens(0L)
                                    .build()
                            )
                            .user("user-1234")
                            .build()
                    )
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseCompletedEvent.Agent.builder().agentName("agent_name").build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseContentPartAdded() {
        val responseContentPartAdded =
            BetaResponseContentPartAddedEvent.builder()
                .contentIndex(0L)
                .itemId("item_id")
                .outputIndex(0L)
                .part(
                    BetaResponseOutputText.builder()
                        .addAnnotation(
                            BetaResponseOutputText.Annotation.FileCitation.builder()
                                .fileId("file_id")
                                .filename("filename")
                                .index(0L)
                                .build()
                        )
                        .text("text")
                        .addLogprob(
                            BetaResponseOutputText.Logprob.builder()
                                .token("token")
                                .addByte(0L)
                                .logprob(0.0)
                                .addTopLogprob(
                                    BetaResponseOutputText.Logprob.TopLogprob.builder()
                                        .token("token")
                                        .addByte(0L)
                                        .logprob(0.0)
                                        .build()
                                )
                                .build()
                        )
                        .build()
                )
                .sequenceNumber(0L)
                .agent(
                    BetaResponseContentPartAddedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseContentPartAdded(responseContentPartAdded)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded())
            .contains(responseContentPartAdded)
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseContentPartAddedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseContentPartAdded(
                BetaResponseContentPartAddedEvent.builder()
                    .contentIndex(0L)
                    .itemId("item_id")
                    .outputIndex(0L)
                    .part(
                        BetaResponseOutputText.builder()
                            .addAnnotation(
                                BetaResponseOutputText.Annotation.FileCitation.builder()
                                    .fileId("file_id")
                                    .filename("filename")
                                    .index(0L)
                                    .build()
                            )
                            .text("text")
                            .addLogprob(
                                BetaResponseOutputText.Logprob.builder()
                                    .token("token")
                                    .addByte(0L)
                                    .logprob(0.0)
                                    .addTopLogprob(
                                        BetaResponseOutputText.Logprob.TopLogprob.builder()
                                            .token("token")
                                            .addByte(0L)
                                            .logprob(0.0)
                                            .build()
                                    )
                                    .build()
                            )
                            .build()
                    )
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseContentPartAddedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseContentPartDone() {
        val responseContentPartDone =
            BetaResponseContentPartDoneEvent.builder()
                .contentIndex(0L)
                .itemId("item_id")
                .outputIndex(0L)
                .part(
                    BetaResponseOutputText.builder()
                        .addAnnotation(
                            BetaResponseOutputText.Annotation.FileCitation.builder()
                                .fileId("file_id")
                                .filename("filename")
                                .index(0L)
                                .build()
                        )
                        .text("text")
                        .addLogprob(
                            BetaResponseOutputText.Logprob.builder()
                                .token("token")
                                .addByte(0L)
                                .logprob(0.0)
                                .addTopLogprob(
                                    BetaResponseOutputText.Logprob.TopLogprob.builder()
                                        .token("token")
                                        .addByte(0L)
                                        .logprob(0.0)
                                        .build()
                                )
                                .build()
                        )
                        .build()
                )
                .sequenceNumber(0L)
                .agent(
                    BetaResponseContentPartDoneEvent.Agent.builder().agentName("agent_name").build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseContentPartDone(responseContentPartDone)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone())
            .contains(responseContentPartDone)
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseContentPartDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseContentPartDone(
                BetaResponseContentPartDoneEvent.builder()
                    .contentIndex(0L)
                    .itemId("item_id")
                    .outputIndex(0L)
                    .part(
                        BetaResponseOutputText.builder()
                            .addAnnotation(
                                BetaResponseOutputText.Annotation.FileCitation.builder()
                                    .fileId("file_id")
                                    .filename("filename")
                                    .index(0L)
                                    .build()
                            )
                            .text("text")
                            .addLogprob(
                                BetaResponseOutputText.Logprob.builder()
                                    .token("token")
                                    .addByte(0L)
                                    .logprob(0.0)
                                    .addTopLogprob(
                                        BetaResponseOutputText.Logprob.TopLogprob.builder()
                                            .token("token")
                                            .addByte(0L)
                                            .logprob(0.0)
                                            .build()
                                    )
                                    .build()
                            )
                            .build()
                    )
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseContentPartDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCreated() {
        val responseCreated =
            BetaResponseCreatedEvent.builder()
                .response(
                    BetaResponse.builder()
                        .id("id")
                        .createdAt(0.0)
                        .error(
                            BetaResponseError.builder()
                                .code(BetaResponseError.Code.SERVER_ERROR)
                                .message("message")
                                .build()
                        )
                        .incompleteDetails(
                            BetaResponse.IncompleteDetails.builder()
                                .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                .build()
                        )
                        .instructions("string")
                        .metadata(
                            BetaResponse.Metadata.builder()
                                .putAdditionalProperty("foo", JsonValue.from("string"))
                                .build()
                        )
                        .model(BetaResponse.Model.GPT_5_1)
                        .addOutput(
                            BetaResponseOutputMessage.builder()
                                .id("id")
                                .addContent(
                                    BetaResponseOutputText.builder()
                                        .addAnnotation(
                                            BetaResponseOutputText.Annotation.FileCitation.builder()
                                                .fileId("file_id")
                                                .filename("filename")
                                                .index(0L)
                                                .build()
                                        )
                                        .text("text")
                                        .addLogprob(
                                            BetaResponseOutputText.Logprob.builder()
                                                .token("token")
                                                .addByte(0L)
                                                .logprob(0.0)
                                                .addTopLogprob(
                                                    BetaResponseOutputText.Logprob.TopLogprob
                                                        .builder()
                                                        .token("token")
                                                        .addByte(0L)
                                                        .logprob(0.0)
                                                        .build()
                                                )
                                                .build()
                                        )
                                        .build()
                                )
                                .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                .agent(
                                    BetaResponseOutputMessage.Agent.builder()
                                        .agentName("agent_name")
                                        .build()
                                )
                                .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                .build()
                        )
                        .parallelToolCalls(true)
                        .temperature(1.0)
                        .toolChoice(BetaToolChoiceOptions.NONE)
                        .addTool(
                            BetaFunctionTool.builder()
                                .name("name")
                                .parameters(
                                    BetaFunctionTool.Parameters.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .strict(true)
                                .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                .deferLoading(true)
                                .description("description")
                                .outputSchema(
                                    BetaFunctionTool.OutputSchema.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .build()
                        )
                        .topP(1.0)
                        .background(true)
                        .completedAt(0.0)
                        .conversation(BetaResponse.Conversation.builder().id("id").build())
                        .maxOutputTokens(0L)
                        .maxToolCalls(0L)
                        .moderation(
                            BetaResponse.Moderation.builder()
                                .input(
                                    BetaResponse.Moderation.Input.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .output(
                                    BetaResponse.Moderation.Output.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .build()
                        )
                        .previousResponseId("previous_response_id")
                        .prompt(
                            BetaResponsePrompt.builder()
                                .id("id")
                                .variables(
                                    BetaResponsePrompt.Variables.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("string"))
                                        .build()
                                )
                                .version("version")
                                .build()
                        )
                        .promptCacheKey("prompt-cache-key-1234")
                        .promptCacheOptions(
                            BetaResponse.PromptCacheOptions.builder()
                                .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                .build()
                        )
                        .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                        .reasoning(
                            BetaResponse.Reasoning.builder()
                                .context(BetaResponse.Reasoning.Context.AUTO)
                                .effort(BetaResponse.Reasoning.Effort.NONE)
                                .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                .summary(BetaResponse.Reasoning.Summary.AUTO)
                                .build()
                        )
                        .safetyIdentifier("safety-identifier-1234")
                        .serviceTier(BetaResponse.ServiceTier.AUTO)
                        .status(BetaResponseStatus.COMPLETED)
                        .text(
                            BetaResponseTextConfig.builder()
                                .formatText()
                                .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                .build()
                        )
                        .topLogprobs(0L)
                        .truncation(BetaResponse.Truncation.AUTO)
                        .usage(
                            BetaResponseUsage.builder()
                                .inputTokens(0L)
                                .inputTokensDetails(
                                    BetaResponseUsage.InputTokensDetails.builder()
                                        .cacheWriteTokens(0L)
                                        .cachedTokens(0L)
                                        .build()
                                )
                                .outputTokens(0L)
                                .outputTokensDetails(
                                    BetaResponseUsage.OutputTokensDetails.builder()
                                        .reasoningTokens(0L)
                                        .build()
                                )
                                .totalTokens(0L)
                                .build()
                        )
                        .user("user-1234")
                        .build()
                )
                .sequenceNumber(0L)
                .agent(BetaResponseCreatedEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent = BetaResponseStreamEvent.ofResponseCreated(responseCreated)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).contains(responseCreated)
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseCreatedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCreated(
                BetaResponseCreatedEvent.builder()
                    .response(
                        BetaResponse.builder()
                            .id("id")
                            .createdAt(0.0)
                            .error(
                                BetaResponseError.builder()
                                    .code(BetaResponseError.Code.SERVER_ERROR)
                                    .message("message")
                                    .build()
                            )
                            .incompleteDetails(
                                BetaResponse.IncompleteDetails.builder()
                                    .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                    .build()
                            )
                            .instructions("string")
                            .metadata(
                                BetaResponse.Metadata.builder()
                                    .putAdditionalProperty("foo", JsonValue.from("string"))
                                    .build()
                            )
                            .model(BetaResponse.Model.GPT_5_1)
                            .addOutput(
                                BetaResponseOutputMessage.builder()
                                    .id("id")
                                    .addContent(
                                        BetaResponseOutputText.builder()
                                            .addAnnotation(
                                                BetaResponseOutputText.Annotation.FileCitation
                                                    .builder()
                                                    .fileId("file_id")
                                                    .filename("filename")
                                                    .index(0L)
                                                    .build()
                                            )
                                            .text("text")
                                            .addLogprob(
                                                BetaResponseOutputText.Logprob.builder()
                                                    .token("token")
                                                    .addByte(0L)
                                                    .logprob(0.0)
                                                    .addTopLogprob(
                                                        BetaResponseOutputText.Logprob.TopLogprob
                                                            .builder()
                                                            .token("token")
                                                            .addByte(0L)
                                                            .logprob(0.0)
                                                            .build()
                                                    )
                                                    .build()
                                            )
                                            .build()
                                    )
                                    .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                    .agent(
                                        BetaResponseOutputMessage.Agent.builder()
                                            .agentName("agent_name")
                                            .build()
                                    )
                                    .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                    .build()
                            )
                            .parallelToolCalls(true)
                            .temperature(1.0)
                            .toolChoice(BetaToolChoiceOptions.NONE)
                            .addTool(
                                BetaFunctionTool.builder()
                                    .name("name")
                                    .parameters(
                                        BetaFunctionTool.Parameters.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .strict(true)
                                    .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                    .deferLoading(true)
                                    .description("description")
                                    .outputSchema(
                                        BetaFunctionTool.OutputSchema.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .build()
                            )
                            .topP(1.0)
                            .background(true)
                            .completedAt(0.0)
                            .conversation(BetaResponse.Conversation.builder().id("id").build())
                            .maxOutputTokens(0L)
                            .maxToolCalls(0L)
                            .moderation(
                                BetaResponse.Moderation.builder()
                                    .input(
                                        BetaResponse.Moderation.Input.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .output(
                                        BetaResponse.Moderation.Output.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .build()
                            )
                            .previousResponseId("previous_response_id")
                            .prompt(
                                BetaResponsePrompt.builder()
                                    .id("id")
                                    .variables(
                                        BetaResponsePrompt.Variables.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("string"))
                                            .build()
                                    )
                                    .version("version")
                                    .build()
                            )
                            .promptCacheKey("prompt-cache-key-1234")
                            .promptCacheOptions(
                                BetaResponse.PromptCacheOptions.builder()
                                    .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                    .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                    .build()
                            )
                            .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                            .reasoning(
                                BetaResponse.Reasoning.builder()
                                    .context(BetaResponse.Reasoning.Context.AUTO)
                                    .effort(BetaResponse.Reasoning.Effort.NONE)
                                    .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                    .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                    .summary(BetaResponse.Reasoning.Summary.AUTO)
                                    .build()
                            )
                            .safetyIdentifier("safety-identifier-1234")
                            .serviceTier(BetaResponse.ServiceTier.AUTO)
                            .status(BetaResponseStatus.COMPLETED)
                            .text(
                                BetaResponseTextConfig.builder()
                                    .formatText()
                                    .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                    .build()
                            )
                            .topLogprobs(0L)
                            .truncation(BetaResponse.Truncation.AUTO)
                            .usage(
                                BetaResponseUsage.builder()
                                    .inputTokens(0L)
                                    .inputTokensDetails(
                                        BetaResponseUsage.InputTokensDetails.builder()
                                            .cacheWriteTokens(0L)
                                            .cachedTokens(0L)
                                            .build()
                                    )
                                    .outputTokens(0L)
                                    .outputTokensDetails(
                                        BetaResponseUsage.OutputTokensDetails.builder()
                                            .reasoningTokens(0L)
                                            .build()
                                    )
                                    .totalTokens(0L)
                                    .build()
                            )
                            .user("user-1234")
                            .build()
                    )
                    .sequenceNumber(0L)
                    .agent(BetaResponseCreatedEvent.Agent.builder().agentName("agent_name").build())
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofError() {
        val error =
            BetaResponseErrorEvent.builder()
                .code("code")
                .message("message")
                .param("param")
                .sequenceNumber(0L)
                .agent(BetaResponseErrorEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent = BetaResponseStreamEvent.ofError(error)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).contains(error)
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofErrorRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofError(
                BetaResponseErrorEvent.builder()
                    .code("code")
                    .message("message")
                    .param("param")
                    .sequenceNumber(0L)
                    .agent(BetaResponseErrorEvent.Agent.builder().agentName("agent_name").build())
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseFileSearchCallCompleted() {
        val responseFileSearchCallCompleted =
            BetaResponseFileSearchCallCompletedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseFileSearchCallCompletedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFileSearchCallCompleted(
                responseFileSearchCallCompleted
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted())
            .contains(responseFileSearchCallCompleted)
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseFileSearchCallCompletedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFileSearchCallCompleted(
                BetaResponseFileSearchCallCompletedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseFileSearchCallCompletedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseFileSearchCallInProgress() {
        val responseFileSearchCallInProgress =
            BetaResponseFileSearchCallInProgressEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseFileSearchCallInProgressEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFileSearchCallInProgress(
                responseFileSearchCallInProgress
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress())
            .contains(responseFileSearchCallInProgress)
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseFileSearchCallInProgressRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFileSearchCallInProgress(
                BetaResponseFileSearchCallInProgressEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseFileSearchCallInProgressEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseFileSearchCallSearching() {
        val responseFileSearchCallSearching =
            BetaResponseFileSearchCallSearchingEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseFileSearchCallSearchingEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFileSearchCallSearching(
                responseFileSearchCallSearching
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching())
            .contains(responseFileSearchCallSearching)
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseFileSearchCallSearchingRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFileSearchCallSearching(
                BetaResponseFileSearchCallSearchingEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseFileSearchCallSearchingEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseFunctionCallArgumentsDelta() {
        val responseFunctionCallArgumentsDelta =
            BetaResponseFunctionCallArgumentsDeltaEvent.builder()
                .delta("delta")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseFunctionCallArgumentsDeltaEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFunctionCallArgumentsDelta(
                responseFunctionCallArgumentsDelta
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta())
            .contains(responseFunctionCallArgumentsDelta)
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseFunctionCallArgumentsDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFunctionCallArgumentsDelta(
                BetaResponseFunctionCallArgumentsDeltaEvent.builder()
                    .delta("delta")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseFunctionCallArgumentsDeltaEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseFunctionCallArgumentsDone() {
        val responseFunctionCallArgumentsDone =
            BetaResponseFunctionCallArgumentsDoneEvent.builder()
                .arguments("arguments")
                .itemId("item_id")
                .name("name")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseFunctionCallArgumentsDoneEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFunctionCallArgumentsDone(
                responseFunctionCallArgumentsDone
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone())
            .contains(responseFunctionCallArgumentsDone)
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseFunctionCallArgumentsDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFunctionCallArgumentsDone(
                BetaResponseFunctionCallArgumentsDoneEvent.builder()
                    .arguments("arguments")
                    .itemId("item_id")
                    .name("name")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseFunctionCallArgumentsDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseInProgress() {
        val responseInProgress =
            BetaResponseInProgressEvent.builder()
                .response(
                    BetaResponse.builder()
                        .id("id")
                        .createdAt(0.0)
                        .error(
                            BetaResponseError.builder()
                                .code(BetaResponseError.Code.SERVER_ERROR)
                                .message("message")
                                .build()
                        )
                        .incompleteDetails(
                            BetaResponse.IncompleteDetails.builder()
                                .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                .build()
                        )
                        .instructions("string")
                        .metadata(
                            BetaResponse.Metadata.builder()
                                .putAdditionalProperty("foo", JsonValue.from("string"))
                                .build()
                        )
                        .model(BetaResponse.Model.GPT_5_1)
                        .addOutput(
                            BetaResponseOutputMessage.builder()
                                .id("id")
                                .addContent(
                                    BetaResponseOutputText.builder()
                                        .addAnnotation(
                                            BetaResponseOutputText.Annotation.FileCitation.builder()
                                                .fileId("file_id")
                                                .filename("filename")
                                                .index(0L)
                                                .build()
                                        )
                                        .text("text")
                                        .addLogprob(
                                            BetaResponseOutputText.Logprob.builder()
                                                .token("token")
                                                .addByte(0L)
                                                .logprob(0.0)
                                                .addTopLogprob(
                                                    BetaResponseOutputText.Logprob.TopLogprob
                                                        .builder()
                                                        .token("token")
                                                        .addByte(0L)
                                                        .logprob(0.0)
                                                        .build()
                                                )
                                                .build()
                                        )
                                        .build()
                                )
                                .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                .agent(
                                    BetaResponseOutputMessage.Agent.builder()
                                        .agentName("agent_name")
                                        .build()
                                )
                                .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                .build()
                        )
                        .parallelToolCalls(true)
                        .temperature(1.0)
                        .toolChoice(BetaToolChoiceOptions.NONE)
                        .addTool(
                            BetaFunctionTool.builder()
                                .name("name")
                                .parameters(
                                    BetaFunctionTool.Parameters.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .strict(true)
                                .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                .deferLoading(true)
                                .description("description")
                                .outputSchema(
                                    BetaFunctionTool.OutputSchema.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .build()
                        )
                        .topP(1.0)
                        .background(true)
                        .completedAt(0.0)
                        .conversation(BetaResponse.Conversation.builder().id("id").build())
                        .maxOutputTokens(0L)
                        .maxToolCalls(0L)
                        .moderation(
                            BetaResponse.Moderation.builder()
                                .input(
                                    BetaResponse.Moderation.Input.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .output(
                                    BetaResponse.Moderation.Output.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .build()
                        )
                        .previousResponseId("previous_response_id")
                        .prompt(
                            BetaResponsePrompt.builder()
                                .id("id")
                                .variables(
                                    BetaResponsePrompt.Variables.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("string"))
                                        .build()
                                )
                                .version("version")
                                .build()
                        )
                        .promptCacheKey("prompt-cache-key-1234")
                        .promptCacheOptions(
                            BetaResponse.PromptCacheOptions.builder()
                                .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                .build()
                        )
                        .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                        .reasoning(
                            BetaResponse.Reasoning.builder()
                                .context(BetaResponse.Reasoning.Context.AUTO)
                                .effort(BetaResponse.Reasoning.Effort.NONE)
                                .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                .summary(BetaResponse.Reasoning.Summary.AUTO)
                                .build()
                        )
                        .safetyIdentifier("safety-identifier-1234")
                        .serviceTier(BetaResponse.ServiceTier.AUTO)
                        .status(BetaResponseStatus.COMPLETED)
                        .text(
                            BetaResponseTextConfig.builder()
                                .formatText()
                                .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                .build()
                        )
                        .topLogprobs(0L)
                        .truncation(BetaResponse.Truncation.AUTO)
                        .usage(
                            BetaResponseUsage.builder()
                                .inputTokens(0L)
                                .inputTokensDetails(
                                    BetaResponseUsage.InputTokensDetails.builder()
                                        .cacheWriteTokens(0L)
                                        .cachedTokens(0L)
                                        .build()
                                )
                                .outputTokens(0L)
                                .outputTokensDetails(
                                    BetaResponseUsage.OutputTokensDetails.builder()
                                        .reasoningTokens(0L)
                                        .build()
                                )
                                .totalTokens(0L)
                                .build()
                        )
                        .user("user-1234")
                        .build()
                )
                .sequenceNumber(0L)
                .agent(BetaResponseInProgressEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseInProgress(responseInProgress)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).contains(responseInProgress)
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseInProgressRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseInProgress(
                BetaResponseInProgressEvent.builder()
                    .response(
                        BetaResponse.builder()
                            .id("id")
                            .createdAt(0.0)
                            .error(
                                BetaResponseError.builder()
                                    .code(BetaResponseError.Code.SERVER_ERROR)
                                    .message("message")
                                    .build()
                            )
                            .incompleteDetails(
                                BetaResponse.IncompleteDetails.builder()
                                    .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                    .build()
                            )
                            .instructions("string")
                            .metadata(
                                BetaResponse.Metadata.builder()
                                    .putAdditionalProperty("foo", JsonValue.from("string"))
                                    .build()
                            )
                            .model(BetaResponse.Model.GPT_5_1)
                            .addOutput(
                                BetaResponseOutputMessage.builder()
                                    .id("id")
                                    .addContent(
                                        BetaResponseOutputText.builder()
                                            .addAnnotation(
                                                BetaResponseOutputText.Annotation.FileCitation
                                                    .builder()
                                                    .fileId("file_id")
                                                    .filename("filename")
                                                    .index(0L)
                                                    .build()
                                            )
                                            .text("text")
                                            .addLogprob(
                                                BetaResponseOutputText.Logprob.builder()
                                                    .token("token")
                                                    .addByte(0L)
                                                    .logprob(0.0)
                                                    .addTopLogprob(
                                                        BetaResponseOutputText.Logprob.TopLogprob
                                                            .builder()
                                                            .token("token")
                                                            .addByte(0L)
                                                            .logprob(0.0)
                                                            .build()
                                                    )
                                                    .build()
                                            )
                                            .build()
                                    )
                                    .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                    .agent(
                                        BetaResponseOutputMessage.Agent.builder()
                                            .agentName("agent_name")
                                            .build()
                                    )
                                    .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                    .build()
                            )
                            .parallelToolCalls(true)
                            .temperature(1.0)
                            .toolChoice(BetaToolChoiceOptions.NONE)
                            .addTool(
                                BetaFunctionTool.builder()
                                    .name("name")
                                    .parameters(
                                        BetaFunctionTool.Parameters.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .strict(true)
                                    .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                    .deferLoading(true)
                                    .description("description")
                                    .outputSchema(
                                        BetaFunctionTool.OutputSchema.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .build()
                            )
                            .topP(1.0)
                            .background(true)
                            .completedAt(0.0)
                            .conversation(BetaResponse.Conversation.builder().id("id").build())
                            .maxOutputTokens(0L)
                            .maxToolCalls(0L)
                            .moderation(
                                BetaResponse.Moderation.builder()
                                    .input(
                                        BetaResponse.Moderation.Input.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .output(
                                        BetaResponse.Moderation.Output.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .build()
                            )
                            .previousResponseId("previous_response_id")
                            .prompt(
                                BetaResponsePrompt.builder()
                                    .id("id")
                                    .variables(
                                        BetaResponsePrompt.Variables.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("string"))
                                            .build()
                                    )
                                    .version("version")
                                    .build()
                            )
                            .promptCacheKey("prompt-cache-key-1234")
                            .promptCacheOptions(
                                BetaResponse.PromptCacheOptions.builder()
                                    .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                    .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                    .build()
                            )
                            .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                            .reasoning(
                                BetaResponse.Reasoning.builder()
                                    .context(BetaResponse.Reasoning.Context.AUTO)
                                    .effort(BetaResponse.Reasoning.Effort.NONE)
                                    .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                    .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                    .summary(BetaResponse.Reasoning.Summary.AUTO)
                                    .build()
                            )
                            .safetyIdentifier("safety-identifier-1234")
                            .serviceTier(BetaResponse.ServiceTier.AUTO)
                            .status(BetaResponseStatus.COMPLETED)
                            .text(
                                BetaResponseTextConfig.builder()
                                    .formatText()
                                    .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                    .build()
                            )
                            .topLogprobs(0L)
                            .truncation(BetaResponse.Truncation.AUTO)
                            .usage(
                                BetaResponseUsage.builder()
                                    .inputTokens(0L)
                                    .inputTokensDetails(
                                        BetaResponseUsage.InputTokensDetails.builder()
                                            .cacheWriteTokens(0L)
                                            .cachedTokens(0L)
                                            .build()
                                    )
                                    .outputTokens(0L)
                                    .outputTokensDetails(
                                        BetaResponseUsage.OutputTokensDetails.builder()
                                            .reasoningTokens(0L)
                                            .build()
                                    )
                                    .totalTokens(0L)
                                    .build()
                            )
                            .user("user-1234")
                            .build()
                    )
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseInProgressEvent.Agent.builder().agentName("agent_name").build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseFailed() {
        val responseFailed =
            BetaResponseFailedEvent.builder()
                .response(
                    BetaResponse.builder()
                        .id("id")
                        .createdAt(0.0)
                        .error(
                            BetaResponseError.builder()
                                .code(BetaResponseError.Code.SERVER_ERROR)
                                .message("message")
                                .build()
                        )
                        .incompleteDetails(
                            BetaResponse.IncompleteDetails.builder()
                                .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                .build()
                        )
                        .instructions("string")
                        .metadata(
                            BetaResponse.Metadata.builder()
                                .putAdditionalProperty("foo", JsonValue.from("string"))
                                .build()
                        )
                        .model(BetaResponse.Model.GPT_5_1)
                        .addOutput(
                            BetaResponseOutputMessage.builder()
                                .id("id")
                                .addContent(
                                    BetaResponseOutputText.builder()
                                        .addAnnotation(
                                            BetaResponseOutputText.Annotation.FileCitation.builder()
                                                .fileId("file_id")
                                                .filename("filename")
                                                .index(0L)
                                                .build()
                                        )
                                        .text("text")
                                        .addLogprob(
                                            BetaResponseOutputText.Logprob.builder()
                                                .token("token")
                                                .addByte(0L)
                                                .logprob(0.0)
                                                .addTopLogprob(
                                                    BetaResponseOutputText.Logprob.TopLogprob
                                                        .builder()
                                                        .token("token")
                                                        .addByte(0L)
                                                        .logprob(0.0)
                                                        .build()
                                                )
                                                .build()
                                        )
                                        .build()
                                )
                                .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                .agent(
                                    BetaResponseOutputMessage.Agent.builder()
                                        .agentName("agent_name")
                                        .build()
                                )
                                .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                .build()
                        )
                        .parallelToolCalls(true)
                        .temperature(1.0)
                        .toolChoice(BetaToolChoiceOptions.NONE)
                        .addTool(
                            BetaFunctionTool.builder()
                                .name("name")
                                .parameters(
                                    BetaFunctionTool.Parameters.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .strict(true)
                                .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                .deferLoading(true)
                                .description("description")
                                .outputSchema(
                                    BetaFunctionTool.OutputSchema.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .build()
                        )
                        .topP(1.0)
                        .background(true)
                        .completedAt(0.0)
                        .conversation(BetaResponse.Conversation.builder().id("id").build())
                        .maxOutputTokens(0L)
                        .maxToolCalls(0L)
                        .moderation(
                            BetaResponse.Moderation.builder()
                                .input(
                                    BetaResponse.Moderation.Input.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .output(
                                    BetaResponse.Moderation.Output.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .build()
                        )
                        .previousResponseId("previous_response_id")
                        .prompt(
                            BetaResponsePrompt.builder()
                                .id("id")
                                .variables(
                                    BetaResponsePrompt.Variables.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("string"))
                                        .build()
                                )
                                .version("version")
                                .build()
                        )
                        .promptCacheKey("prompt-cache-key-1234")
                        .promptCacheOptions(
                            BetaResponse.PromptCacheOptions.builder()
                                .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                .build()
                        )
                        .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                        .reasoning(
                            BetaResponse.Reasoning.builder()
                                .context(BetaResponse.Reasoning.Context.AUTO)
                                .effort(BetaResponse.Reasoning.Effort.NONE)
                                .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                .summary(BetaResponse.Reasoning.Summary.AUTO)
                                .build()
                        )
                        .safetyIdentifier("safety-identifier-1234")
                        .serviceTier(BetaResponse.ServiceTier.AUTO)
                        .status(BetaResponseStatus.COMPLETED)
                        .text(
                            BetaResponseTextConfig.builder()
                                .formatText()
                                .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                .build()
                        )
                        .topLogprobs(0L)
                        .truncation(BetaResponse.Truncation.AUTO)
                        .usage(
                            BetaResponseUsage.builder()
                                .inputTokens(0L)
                                .inputTokensDetails(
                                    BetaResponseUsage.InputTokensDetails.builder()
                                        .cacheWriteTokens(0L)
                                        .cachedTokens(0L)
                                        .build()
                                )
                                .outputTokens(0L)
                                .outputTokensDetails(
                                    BetaResponseUsage.OutputTokensDetails.builder()
                                        .reasoningTokens(0L)
                                        .build()
                                )
                                .totalTokens(0L)
                                .build()
                        )
                        .user("user-1234")
                        .build()
                )
                .sequenceNumber(0L)
                .agent(BetaResponseFailedEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent = BetaResponseStreamEvent.ofResponseFailed(responseFailed)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).contains(responseFailed)
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseFailedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseFailed(
                BetaResponseFailedEvent.builder()
                    .response(
                        BetaResponse.builder()
                            .id("id")
                            .createdAt(0.0)
                            .error(
                                BetaResponseError.builder()
                                    .code(BetaResponseError.Code.SERVER_ERROR)
                                    .message("message")
                                    .build()
                            )
                            .incompleteDetails(
                                BetaResponse.IncompleteDetails.builder()
                                    .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                    .build()
                            )
                            .instructions("string")
                            .metadata(
                                BetaResponse.Metadata.builder()
                                    .putAdditionalProperty("foo", JsonValue.from("string"))
                                    .build()
                            )
                            .model(BetaResponse.Model.GPT_5_1)
                            .addOutput(
                                BetaResponseOutputMessage.builder()
                                    .id("id")
                                    .addContent(
                                        BetaResponseOutputText.builder()
                                            .addAnnotation(
                                                BetaResponseOutputText.Annotation.FileCitation
                                                    .builder()
                                                    .fileId("file_id")
                                                    .filename("filename")
                                                    .index(0L)
                                                    .build()
                                            )
                                            .text("text")
                                            .addLogprob(
                                                BetaResponseOutputText.Logprob.builder()
                                                    .token("token")
                                                    .addByte(0L)
                                                    .logprob(0.0)
                                                    .addTopLogprob(
                                                        BetaResponseOutputText.Logprob.TopLogprob
                                                            .builder()
                                                            .token("token")
                                                            .addByte(0L)
                                                            .logprob(0.0)
                                                            .build()
                                                    )
                                                    .build()
                                            )
                                            .build()
                                    )
                                    .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                    .agent(
                                        BetaResponseOutputMessage.Agent.builder()
                                            .agentName("agent_name")
                                            .build()
                                    )
                                    .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                    .build()
                            )
                            .parallelToolCalls(true)
                            .temperature(1.0)
                            .toolChoice(BetaToolChoiceOptions.NONE)
                            .addTool(
                                BetaFunctionTool.builder()
                                    .name("name")
                                    .parameters(
                                        BetaFunctionTool.Parameters.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .strict(true)
                                    .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                    .deferLoading(true)
                                    .description("description")
                                    .outputSchema(
                                        BetaFunctionTool.OutputSchema.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .build()
                            )
                            .topP(1.0)
                            .background(true)
                            .completedAt(0.0)
                            .conversation(BetaResponse.Conversation.builder().id("id").build())
                            .maxOutputTokens(0L)
                            .maxToolCalls(0L)
                            .moderation(
                                BetaResponse.Moderation.builder()
                                    .input(
                                        BetaResponse.Moderation.Input.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .output(
                                        BetaResponse.Moderation.Output.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .build()
                            )
                            .previousResponseId("previous_response_id")
                            .prompt(
                                BetaResponsePrompt.builder()
                                    .id("id")
                                    .variables(
                                        BetaResponsePrompt.Variables.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("string"))
                                            .build()
                                    )
                                    .version("version")
                                    .build()
                            )
                            .promptCacheKey("prompt-cache-key-1234")
                            .promptCacheOptions(
                                BetaResponse.PromptCacheOptions.builder()
                                    .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                    .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                    .build()
                            )
                            .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                            .reasoning(
                                BetaResponse.Reasoning.builder()
                                    .context(BetaResponse.Reasoning.Context.AUTO)
                                    .effort(BetaResponse.Reasoning.Effort.NONE)
                                    .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                    .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                    .summary(BetaResponse.Reasoning.Summary.AUTO)
                                    .build()
                            )
                            .safetyIdentifier("safety-identifier-1234")
                            .serviceTier(BetaResponse.ServiceTier.AUTO)
                            .status(BetaResponseStatus.COMPLETED)
                            .text(
                                BetaResponseTextConfig.builder()
                                    .formatText()
                                    .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                    .build()
                            )
                            .topLogprobs(0L)
                            .truncation(BetaResponse.Truncation.AUTO)
                            .usage(
                                BetaResponseUsage.builder()
                                    .inputTokens(0L)
                                    .inputTokensDetails(
                                        BetaResponseUsage.InputTokensDetails.builder()
                                            .cacheWriteTokens(0L)
                                            .cachedTokens(0L)
                                            .build()
                                    )
                                    .outputTokens(0L)
                                    .outputTokensDetails(
                                        BetaResponseUsage.OutputTokensDetails.builder()
                                            .reasoningTokens(0L)
                                            .build()
                                    )
                                    .totalTokens(0L)
                                    .build()
                            )
                            .user("user-1234")
                            .build()
                    )
                    .sequenceNumber(0L)
                    .agent(BetaResponseFailedEvent.Agent.builder().agentName("agent_name").build())
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseIncomplete() {
        val responseIncomplete =
            BetaResponseIncompleteEvent.builder()
                .response(
                    BetaResponse.builder()
                        .id("id")
                        .createdAt(0.0)
                        .error(
                            BetaResponseError.builder()
                                .code(BetaResponseError.Code.SERVER_ERROR)
                                .message("message")
                                .build()
                        )
                        .incompleteDetails(
                            BetaResponse.IncompleteDetails.builder()
                                .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                .build()
                        )
                        .instructions("string")
                        .metadata(
                            BetaResponse.Metadata.builder()
                                .putAdditionalProperty("foo", JsonValue.from("string"))
                                .build()
                        )
                        .model(BetaResponse.Model.GPT_5_1)
                        .addOutput(
                            BetaResponseOutputMessage.builder()
                                .id("id")
                                .addContent(
                                    BetaResponseOutputText.builder()
                                        .addAnnotation(
                                            BetaResponseOutputText.Annotation.FileCitation.builder()
                                                .fileId("file_id")
                                                .filename("filename")
                                                .index(0L)
                                                .build()
                                        )
                                        .text("text")
                                        .addLogprob(
                                            BetaResponseOutputText.Logprob.builder()
                                                .token("token")
                                                .addByte(0L)
                                                .logprob(0.0)
                                                .addTopLogprob(
                                                    BetaResponseOutputText.Logprob.TopLogprob
                                                        .builder()
                                                        .token("token")
                                                        .addByte(0L)
                                                        .logprob(0.0)
                                                        .build()
                                                )
                                                .build()
                                        )
                                        .build()
                                )
                                .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                .agent(
                                    BetaResponseOutputMessage.Agent.builder()
                                        .agentName("agent_name")
                                        .build()
                                )
                                .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                .build()
                        )
                        .parallelToolCalls(true)
                        .temperature(1.0)
                        .toolChoice(BetaToolChoiceOptions.NONE)
                        .addTool(
                            BetaFunctionTool.builder()
                                .name("name")
                                .parameters(
                                    BetaFunctionTool.Parameters.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .strict(true)
                                .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                .deferLoading(true)
                                .description("description")
                                .outputSchema(
                                    BetaFunctionTool.OutputSchema.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .build()
                        )
                        .topP(1.0)
                        .background(true)
                        .completedAt(0.0)
                        .conversation(BetaResponse.Conversation.builder().id("id").build())
                        .maxOutputTokens(0L)
                        .maxToolCalls(0L)
                        .moderation(
                            BetaResponse.Moderation.builder()
                                .input(
                                    BetaResponse.Moderation.Input.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .output(
                                    BetaResponse.Moderation.Output.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .build()
                        )
                        .previousResponseId("previous_response_id")
                        .prompt(
                            BetaResponsePrompt.builder()
                                .id("id")
                                .variables(
                                    BetaResponsePrompt.Variables.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("string"))
                                        .build()
                                )
                                .version("version")
                                .build()
                        )
                        .promptCacheKey("prompt-cache-key-1234")
                        .promptCacheOptions(
                            BetaResponse.PromptCacheOptions.builder()
                                .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                .build()
                        )
                        .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                        .reasoning(
                            BetaResponse.Reasoning.builder()
                                .context(BetaResponse.Reasoning.Context.AUTO)
                                .effort(BetaResponse.Reasoning.Effort.NONE)
                                .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                .summary(BetaResponse.Reasoning.Summary.AUTO)
                                .build()
                        )
                        .safetyIdentifier("safety-identifier-1234")
                        .serviceTier(BetaResponse.ServiceTier.AUTO)
                        .status(BetaResponseStatus.COMPLETED)
                        .text(
                            BetaResponseTextConfig.builder()
                                .formatText()
                                .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                .build()
                        )
                        .topLogprobs(0L)
                        .truncation(BetaResponse.Truncation.AUTO)
                        .usage(
                            BetaResponseUsage.builder()
                                .inputTokens(0L)
                                .inputTokensDetails(
                                    BetaResponseUsage.InputTokensDetails.builder()
                                        .cacheWriteTokens(0L)
                                        .cachedTokens(0L)
                                        .build()
                                )
                                .outputTokens(0L)
                                .outputTokensDetails(
                                    BetaResponseUsage.OutputTokensDetails.builder()
                                        .reasoningTokens(0L)
                                        .build()
                                )
                                .totalTokens(0L)
                                .build()
                        )
                        .user("user-1234")
                        .build()
                )
                .sequenceNumber(0L)
                .agent(BetaResponseIncompleteEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseIncomplete(responseIncomplete)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).contains(responseIncomplete)
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseIncompleteRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseIncomplete(
                BetaResponseIncompleteEvent.builder()
                    .response(
                        BetaResponse.builder()
                            .id("id")
                            .createdAt(0.0)
                            .error(
                                BetaResponseError.builder()
                                    .code(BetaResponseError.Code.SERVER_ERROR)
                                    .message("message")
                                    .build()
                            )
                            .incompleteDetails(
                                BetaResponse.IncompleteDetails.builder()
                                    .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                    .build()
                            )
                            .instructions("string")
                            .metadata(
                                BetaResponse.Metadata.builder()
                                    .putAdditionalProperty("foo", JsonValue.from("string"))
                                    .build()
                            )
                            .model(BetaResponse.Model.GPT_5_1)
                            .addOutput(
                                BetaResponseOutputMessage.builder()
                                    .id("id")
                                    .addContent(
                                        BetaResponseOutputText.builder()
                                            .addAnnotation(
                                                BetaResponseOutputText.Annotation.FileCitation
                                                    .builder()
                                                    .fileId("file_id")
                                                    .filename("filename")
                                                    .index(0L)
                                                    .build()
                                            )
                                            .text("text")
                                            .addLogprob(
                                                BetaResponseOutputText.Logprob.builder()
                                                    .token("token")
                                                    .addByte(0L)
                                                    .logprob(0.0)
                                                    .addTopLogprob(
                                                        BetaResponseOutputText.Logprob.TopLogprob
                                                            .builder()
                                                            .token("token")
                                                            .addByte(0L)
                                                            .logprob(0.0)
                                                            .build()
                                                    )
                                                    .build()
                                            )
                                            .build()
                                    )
                                    .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                    .agent(
                                        BetaResponseOutputMessage.Agent.builder()
                                            .agentName("agent_name")
                                            .build()
                                    )
                                    .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                    .build()
                            )
                            .parallelToolCalls(true)
                            .temperature(1.0)
                            .toolChoice(BetaToolChoiceOptions.NONE)
                            .addTool(
                                BetaFunctionTool.builder()
                                    .name("name")
                                    .parameters(
                                        BetaFunctionTool.Parameters.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .strict(true)
                                    .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                    .deferLoading(true)
                                    .description("description")
                                    .outputSchema(
                                        BetaFunctionTool.OutputSchema.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .build()
                            )
                            .topP(1.0)
                            .background(true)
                            .completedAt(0.0)
                            .conversation(BetaResponse.Conversation.builder().id("id").build())
                            .maxOutputTokens(0L)
                            .maxToolCalls(0L)
                            .moderation(
                                BetaResponse.Moderation.builder()
                                    .input(
                                        BetaResponse.Moderation.Input.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .output(
                                        BetaResponse.Moderation.Output.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .build()
                            )
                            .previousResponseId("previous_response_id")
                            .prompt(
                                BetaResponsePrompt.builder()
                                    .id("id")
                                    .variables(
                                        BetaResponsePrompt.Variables.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("string"))
                                            .build()
                                    )
                                    .version("version")
                                    .build()
                            )
                            .promptCacheKey("prompt-cache-key-1234")
                            .promptCacheOptions(
                                BetaResponse.PromptCacheOptions.builder()
                                    .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                    .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                    .build()
                            )
                            .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                            .reasoning(
                                BetaResponse.Reasoning.builder()
                                    .context(BetaResponse.Reasoning.Context.AUTO)
                                    .effort(BetaResponse.Reasoning.Effort.NONE)
                                    .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                    .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                    .summary(BetaResponse.Reasoning.Summary.AUTO)
                                    .build()
                            )
                            .safetyIdentifier("safety-identifier-1234")
                            .serviceTier(BetaResponse.ServiceTier.AUTO)
                            .status(BetaResponseStatus.COMPLETED)
                            .text(
                                BetaResponseTextConfig.builder()
                                    .formatText()
                                    .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                    .build()
                            )
                            .topLogprobs(0L)
                            .truncation(BetaResponse.Truncation.AUTO)
                            .usage(
                                BetaResponseUsage.builder()
                                    .inputTokens(0L)
                                    .inputTokensDetails(
                                        BetaResponseUsage.InputTokensDetails.builder()
                                            .cacheWriteTokens(0L)
                                            .cachedTokens(0L)
                                            .build()
                                    )
                                    .outputTokens(0L)
                                    .outputTokensDetails(
                                        BetaResponseUsage.OutputTokensDetails.builder()
                                            .reasoningTokens(0L)
                                            .build()
                                    )
                                    .totalTokens(0L)
                                    .build()
                            )
                            .user("user-1234")
                            .build()
                    )
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseIncompleteEvent.Agent.builder().agentName("agent_name").build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseOutputItemAdded() {
        val responseOutputItemAdded =
            BetaResponseOutputItemAddedEvent.builder()
                .item(
                    BetaResponseOutputMessage.builder()
                        .id("id")
                        .addContent(
                            BetaResponseOutputText.builder()
                                .addAnnotation(
                                    BetaResponseOutputText.Annotation.FileCitation.builder()
                                        .fileId("file_id")
                                        .filename("filename")
                                        .index(0L)
                                        .build()
                                )
                                .text("text")
                                .addLogprob(
                                    BetaResponseOutputText.Logprob.builder()
                                        .token("token")
                                        .addByte(0L)
                                        .logprob(0.0)
                                        .addTopLogprob(
                                            BetaResponseOutputText.Logprob.TopLogprob.builder()
                                                .token("token")
                                                .addByte(0L)
                                                .logprob(0.0)
                                                .build()
                                        )
                                        .build()
                                )
                                .build()
                        )
                        .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                        .agent(
                            BetaResponseOutputMessage.Agent.builder()
                                .agentName("agent_name")
                                .build()
                        )
                        .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                        .build()
                )
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseOutputItemAddedEvent.Agent.builder().agentName("agent_name").build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputItemAdded(responseOutputItemAdded)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded())
            .contains(responseOutputItemAdded)
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseOutputItemAddedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputItemAdded(
                BetaResponseOutputItemAddedEvent.builder()
                    .item(
                        BetaResponseOutputMessage.builder()
                            .id("id")
                            .addContent(
                                BetaResponseOutputText.builder()
                                    .addAnnotation(
                                        BetaResponseOutputText.Annotation.FileCitation.builder()
                                            .fileId("file_id")
                                            .filename("filename")
                                            .index(0L)
                                            .build()
                                    )
                                    .text("text")
                                    .addLogprob(
                                        BetaResponseOutputText.Logprob.builder()
                                            .token("token")
                                            .addByte(0L)
                                            .logprob(0.0)
                                            .addTopLogprob(
                                                BetaResponseOutputText.Logprob.TopLogprob.builder()
                                                    .token("token")
                                                    .addByte(0L)
                                                    .logprob(0.0)
                                                    .build()
                                            )
                                            .build()
                                    )
                                    .build()
                            )
                            .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                            .agent(
                                BetaResponseOutputMessage.Agent.builder()
                                    .agentName("agent_name")
                                    .build()
                            )
                            .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                            .build()
                    )
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseOutputItemAddedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseOutputItemDone() {
        val responseOutputItemDone =
            BetaResponseOutputItemDoneEvent.builder()
                .item(
                    BetaResponseOutputMessage.builder()
                        .id("id")
                        .addContent(
                            BetaResponseOutputText.builder()
                                .addAnnotation(
                                    BetaResponseOutputText.Annotation.FileCitation.builder()
                                        .fileId("file_id")
                                        .filename("filename")
                                        .index(0L)
                                        .build()
                                )
                                .text("text")
                                .addLogprob(
                                    BetaResponseOutputText.Logprob.builder()
                                        .token("token")
                                        .addByte(0L)
                                        .logprob(0.0)
                                        .addTopLogprob(
                                            BetaResponseOutputText.Logprob.TopLogprob.builder()
                                                .token("token")
                                                .addByte(0L)
                                                .logprob(0.0)
                                                .build()
                                        )
                                        .build()
                                )
                                .build()
                        )
                        .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                        .agent(
                            BetaResponseOutputMessage.Agent.builder()
                                .agentName("agent_name")
                                .build()
                        )
                        .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                        .build()
                )
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseOutputItemDoneEvent.Agent.builder().agentName("agent_name").build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputItemDone(responseOutputItemDone)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone())
            .contains(responseOutputItemDone)
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseOutputItemDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputItemDone(
                BetaResponseOutputItemDoneEvent.builder()
                    .item(
                        BetaResponseOutputMessage.builder()
                            .id("id")
                            .addContent(
                                BetaResponseOutputText.builder()
                                    .addAnnotation(
                                        BetaResponseOutputText.Annotation.FileCitation.builder()
                                            .fileId("file_id")
                                            .filename("filename")
                                            .index(0L)
                                            .build()
                                    )
                                    .text("text")
                                    .addLogprob(
                                        BetaResponseOutputText.Logprob.builder()
                                            .token("token")
                                            .addByte(0L)
                                            .logprob(0.0)
                                            .addTopLogprob(
                                                BetaResponseOutputText.Logprob.TopLogprob.builder()
                                                    .token("token")
                                                    .addByte(0L)
                                                    .logprob(0.0)
                                                    .build()
                                            )
                                            .build()
                                    )
                                    .build()
                            )
                            .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                            .agent(
                                BetaResponseOutputMessage.Agent.builder()
                                    .agentName("agent_name")
                                    .build()
                            )
                            .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                            .build()
                    )
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseOutputItemDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseReasoningSummaryPartAdded() {
        val responseReasoningSummaryPartAdded =
            BetaResponseReasoningSummaryPartAddedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .part(
                    BetaResponseReasoningSummaryPartAddedEvent.Part.builder().text("text").build()
                )
                .sequenceNumber(0L)
                .summaryIndex(0L)
                .agent(
                    BetaResponseReasoningSummaryPartAddedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningSummaryPartAdded(
                responseReasoningSummaryPartAdded
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded())
            .contains(responseReasoningSummaryPartAdded)
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseReasoningSummaryPartAddedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningSummaryPartAdded(
                BetaResponseReasoningSummaryPartAddedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .part(
                        BetaResponseReasoningSummaryPartAddedEvent.Part.builder()
                            .text("text")
                            .build()
                    )
                    .sequenceNumber(0L)
                    .summaryIndex(0L)
                    .agent(
                        BetaResponseReasoningSummaryPartAddedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseReasoningSummaryPartDone() {
        val responseReasoningSummaryPartDone =
            BetaResponseReasoningSummaryPartDoneEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .part(BetaResponseReasoningSummaryPartDoneEvent.Part.builder().text("text").build())
                .sequenceNumber(0L)
                .summaryIndex(0L)
                .agent(
                    BetaResponseReasoningSummaryPartDoneEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .status(BetaResponseReasoningSummaryPartDoneEvent.Status.INCOMPLETE)
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningSummaryPartDone(
                responseReasoningSummaryPartDone
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone())
            .contains(responseReasoningSummaryPartDone)
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseReasoningSummaryPartDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningSummaryPartDone(
                BetaResponseReasoningSummaryPartDoneEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .part(
                        BetaResponseReasoningSummaryPartDoneEvent.Part.builder()
                            .text("text")
                            .build()
                    )
                    .sequenceNumber(0L)
                    .summaryIndex(0L)
                    .agent(
                        BetaResponseReasoningSummaryPartDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .status(BetaResponseReasoningSummaryPartDoneEvent.Status.INCOMPLETE)
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseReasoningSummaryTextDelta() {
        val responseReasoningSummaryTextDelta =
            BetaResponseReasoningSummaryTextDeltaEvent.builder()
                .delta("delta")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .summaryIndex(0L)
                .agent(
                    BetaResponseReasoningSummaryTextDeltaEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningSummaryTextDelta(
                responseReasoningSummaryTextDelta
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta())
            .contains(responseReasoningSummaryTextDelta)
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseReasoningSummaryTextDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningSummaryTextDelta(
                BetaResponseReasoningSummaryTextDeltaEvent.builder()
                    .delta("delta")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .summaryIndex(0L)
                    .agent(
                        BetaResponseReasoningSummaryTextDeltaEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseReasoningSummaryTextDone() {
        val responseReasoningSummaryTextDone =
            BetaResponseReasoningSummaryTextDoneEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .summaryIndex(0L)
                .text("text")
                .agent(
                    BetaResponseReasoningSummaryTextDoneEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningSummaryTextDone(
                responseReasoningSummaryTextDone
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone())
            .contains(responseReasoningSummaryTextDone)
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseReasoningSummaryTextDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningSummaryTextDone(
                BetaResponseReasoningSummaryTextDoneEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .summaryIndex(0L)
                    .text("text")
                    .agent(
                        BetaResponseReasoningSummaryTextDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseReasoningTextDelta() {
        val responseReasoningTextDelta =
            BetaResponseReasoningTextDeltaEvent.builder()
                .contentIndex(0L)
                .delta("delta")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseReasoningTextDeltaEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningTextDelta(responseReasoningTextDelta)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta())
            .contains(responseReasoningTextDelta)
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseReasoningTextDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningTextDelta(
                BetaResponseReasoningTextDeltaEvent.builder()
                    .contentIndex(0L)
                    .delta("delta")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseReasoningTextDeltaEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseReasoningTextDone() {
        val responseReasoningTextDone =
            BetaResponseReasoningTextDoneEvent.builder()
                .contentIndex(0L)
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .text("text")
                .agent(
                    BetaResponseReasoningTextDoneEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningTextDone(responseReasoningTextDone)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone())
            .contains(responseReasoningTextDone)
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseReasoningTextDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseReasoningTextDone(
                BetaResponseReasoningTextDoneEvent.builder()
                    .contentIndex(0L)
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .text("text")
                    .agent(
                        BetaResponseReasoningTextDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseRefusalDelta() {
        val responseRefusalDelta =
            BetaResponseRefusalDeltaEvent.builder()
                .contentIndex(0L)
                .delta("delta")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseRefusalDeltaEvent.Agent.builder().agentName("agent_name").build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseRefusalDelta(responseRefusalDelta)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).contains(responseRefusalDelta)
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseRefusalDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseRefusalDelta(
                BetaResponseRefusalDeltaEvent.builder()
                    .contentIndex(0L)
                    .delta("delta")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseRefusalDeltaEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseRefusalDone() {
        val responseRefusalDone =
            BetaResponseRefusalDoneEvent.builder()
                .contentIndex(0L)
                .itemId("item_id")
                .outputIndex(0L)
                .refusal("refusal")
                .sequenceNumber(0L)
                .agent(BetaResponseRefusalDoneEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseRefusalDone(responseRefusalDone)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).contains(responseRefusalDone)
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseRefusalDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseRefusalDone(
                BetaResponseRefusalDoneEvent.builder()
                    .contentIndex(0L)
                    .itemId("item_id")
                    .outputIndex(0L)
                    .refusal("refusal")
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseRefusalDoneEvent.Agent.builder().agentName("agent_name").build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseOutputTextDelta() {
        val responseOutputTextDelta =
            BetaResponseTextDeltaEvent.builder()
                .contentIndex(0L)
                .delta("delta")
                .itemId("item_id")
                .addLogprob(
                    BetaResponseTextDeltaEvent.Logprob.builder()
                        .token("token")
                        .logprob(0.0)
                        .addTopLogprob(
                            BetaResponseTextDeltaEvent.Logprob.TopLogprob.builder()
                                .token("token")
                                .logprob(0.0)
                                .build()
                        )
                        .build()
                )
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(BetaResponseTextDeltaEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputTextDelta(responseOutputTextDelta)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta())
            .contains(responseOutputTextDelta)
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseOutputTextDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputTextDelta(
                BetaResponseTextDeltaEvent.builder()
                    .contentIndex(0L)
                    .delta("delta")
                    .itemId("item_id")
                    .addLogprob(
                        BetaResponseTextDeltaEvent.Logprob.builder()
                            .token("token")
                            .logprob(0.0)
                            .addTopLogprob(
                                BetaResponseTextDeltaEvent.Logprob.TopLogprob.builder()
                                    .token("token")
                                    .logprob(0.0)
                                    .build()
                            )
                            .build()
                    )
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseTextDeltaEvent.Agent.builder().agentName("agent_name").build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseOutputTextDone() {
        val responseOutputTextDone =
            BetaResponseTextDoneEvent.builder()
                .contentIndex(0L)
                .itemId("item_id")
                .addLogprob(
                    BetaResponseTextDoneEvent.Logprob.builder()
                        .token("token")
                        .logprob(0.0)
                        .addTopLogprob(
                            BetaResponseTextDoneEvent.Logprob.TopLogprob.builder()
                                .token("token")
                                .logprob(0.0)
                                .build()
                        )
                        .build()
                )
                .outputIndex(0L)
                .sequenceNumber(0L)
                .text("text")
                .agent(BetaResponseTextDoneEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputTextDone(responseOutputTextDone)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone())
            .contains(responseOutputTextDone)
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseOutputTextDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputTextDone(
                BetaResponseTextDoneEvent.builder()
                    .contentIndex(0L)
                    .itemId("item_id")
                    .addLogprob(
                        BetaResponseTextDoneEvent.Logprob.builder()
                            .token("token")
                            .logprob(0.0)
                            .addTopLogprob(
                                BetaResponseTextDoneEvent.Logprob.TopLogprob.builder()
                                    .token("token")
                                    .logprob(0.0)
                                    .build()
                            )
                            .build()
                    )
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .text("text")
                    .agent(
                        BetaResponseTextDoneEvent.Agent.builder().agentName("agent_name").build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseWebSearchCallCompleted() {
        val responseWebSearchCallCompleted =
            BetaResponseWebSearchCallCompletedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseWebSearchCallCompletedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseWebSearchCallCompleted(responseWebSearchCallCompleted)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted())
            .contains(responseWebSearchCallCompleted)
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseWebSearchCallCompletedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseWebSearchCallCompleted(
                BetaResponseWebSearchCallCompletedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseWebSearchCallCompletedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseWebSearchCallInProgress() {
        val responseWebSearchCallInProgress =
            BetaResponseWebSearchCallInProgressEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseWebSearchCallInProgressEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseWebSearchCallInProgress(
                responseWebSearchCallInProgress
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress())
            .contains(responseWebSearchCallInProgress)
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseWebSearchCallInProgressRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseWebSearchCallInProgress(
                BetaResponseWebSearchCallInProgressEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseWebSearchCallInProgressEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseWebSearchCallSearching() {
        val responseWebSearchCallSearching =
            BetaResponseWebSearchCallSearchingEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseWebSearchCallSearchingEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseWebSearchCallSearching(responseWebSearchCallSearching)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching())
            .contains(responseWebSearchCallSearching)
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseWebSearchCallSearchingRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseWebSearchCallSearching(
                BetaResponseWebSearchCallSearchingEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseWebSearchCallSearchingEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseImageGenerationCallCompleted() {
        val responseImageGenerationCallCompleted =
            BetaResponseImageGenCallCompletedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseImageGenCallCompletedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseImageGenerationCallCompleted(
                responseImageGenerationCallCompleted
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted())
            .contains(responseImageGenerationCallCompleted)
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseImageGenerationCallCompletedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseImageGenerationCallCompleted(
                BetaResponseImageGenCallCompletedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseImageGenCallCompletedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseImageGenerationCallGenerating() {
        val responseImageGenerationCallGenerating =
            BetaResponseImageGenCallGeneratingEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseImageGenCallGeneratingEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseImageGenerationCallGenerating(
                responseImageGenerationCallGenerating
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating())
            .contains(responseImageGenerationCallGenerating)
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseImageGenerationCallGeneratingRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseImageGenerationCallGenerating(
                BetaResponseImageGenCallGeneratingEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseImageGenCallGeneratingEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseImageGenerationCallInProgress() {
        val responseImageGenerationCallInProgress =
            BetaResponseImageGenCallInProgressEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseImageGenCallInProgressEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseImageGenerationCallInProgress(
                responseImageGenerationCallInProgress
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress())
            .contains(responseImageGenerationCallInProgress)
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseImageGenerationCallInProgressRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseImageGenerationCallInProgress(
                BetaResponseImageGenCallInProgressEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseImageGenCallInProgressEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseImageGenerationCallPartialImage() {
        val responseImageGenerationCallPartialImage =
            BetaResponseImageGenCallPartialImageEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .partialImageB64("partial_image_b64")
                .partialImageIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseImageGenCallPartialImageEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseImageGenerationCallPartialImage(
                responseImageGenerationCallPartialImage
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage())
            .contains(responseImageGenerationCallPartialImage)
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseImageGenerationCallPartialImageRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseImageGenerationCallPartialImage(
                BetaResponseImageGenCallPartialImageEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .partialImageB64("partial_image_b64")
                    .partialImageIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseImageGenCallPartialImageEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseMcpCallArgumentsDelta() {
        val responseMcpCallArgumentsDelta =
            BetaResponseMcpCallArgumentsDeltaEvent.builder()
                .delta("delta")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseMcpCallArgumentsDeltaEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallArgumentsDelta(responseMcpCallArgumentsDelta)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta())
            .contains(responseMcpCallArgumentsDelta)
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseMcpCallArgumentsDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallArgumentsDelta(
                BetaResponseMcpCallArgumentsDeltaEvent.builder()
                    .delta("delta")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseMcpCallArgumentsDeltaEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseMcpCallArgumentsDone() {
        val responseMcpCallArgumentsDone =
            BetaResponseMcpCallArgumentsDoneEvent.builder()
                .arguments("arguments")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseMcpCallArgumentsDoneEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallArgumentsDone(responseMcpCallArgumentsDone)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone())
            .contains(responseMcpCallArgumentsDone)
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseMcpCallArgumentsDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallArgumentsDone(
                BetaResponseMcpCallArgumentsDoneEvent.builder()
                    .arguments("arguments")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseMcpCallArgumentsDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseMcpCallCompleted() {
        val responseMcpCallCompleted =
            BetaResponseMcpCallCompletedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseMcpCallCompletedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallCompleted(responseMcpCallCompleted)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted())
            .contains(responseMcpCallCompleted)
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseMcpCallCompletedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallCompleted(
                BetaResponseMcpCallCompletedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseMcpCallCompletedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseMcpCallFailed() {
        val responseMcpCallFailed =
            BetaResponseMcpCallFailedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseMcpCallFailedEvent.Agent.builder().agentName("agent_name").build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallFailed(responseMcpCallFailed)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).contains(responseMcpCallFailed)
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseMcpCallFailedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallFailed(
                BetaResponseMcpCallFailedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseMcpCallFailedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseMcpCallInProgress() {
        val responseMcpCallInProgress =
            BetaResponseMcpCallInProgressEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseMcpCallInProgressEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallInProgress(responseMcpCallInProgress)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress())
            .contains(responseMcpCallInProgress)
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseMcpCallInProgressRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpCallInProgress(
                BetaResponseMcpCallInProgressEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseMcpCallInProgressEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseMcpListToolsCompleted() {
        val responseMcpListToolsCompleted =
            BetaResponseMcpListToolsCompletedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseMcpListToolsCompletedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpListToolsCompleted(responseMcpListToolsCompleted)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted())
            .contains(responseMcpListToolsCompleted)
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseMcpListToolsCompletedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpListToolsCompleted(
                BetaResponseMcpListToolsCompletedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseMcpListToolsCompletedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseMcpListToolsFailed() {
        val responseMcpListToolsFailed =
            BetaResponseMcpListToolsFailedEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseMcpListToolsFailedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpListToolsFailed(responseMcpListToolsFailed)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed())
            .contains(responseMcpListToolsFailed)
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseMcpListToolsFailedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpListToolsFailed(
                BetaResponseMcpListToolsFailedEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseMcpListToolsFailedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseMcpListToolsInProgress() {
        val responseMcpListToolsInProgress =
            BetaResponseMcpListToolsInProgressEvent.builder()
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseMcpListToolsInProgressEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpListToolsInProgress(responseMcpListToolsInProgress)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress())
            .contains(responseMcpListToolsInProgress)
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseMcpListToolsInProgressRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseMcpListToolsInProgress(
                BetaResponseMcpListToolsInProgressEvent.builder()
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseMcpListToolsInProgressEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseOutputTextAnnotationAdded() {
        val responseOutputTextAnnotationAdded =
            BetaResponseOutputTextAnnotationAddedEvent.builder()
                .annotation(JsonValue.from(mapOf<String, Any>()))
                .annotationIndex(0L)
                .contentIndex(0L)
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseOutputTextAnnotationAddedEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputTextAnnotationAdded(
                responseOutputTextAnnotationAdded
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded())
            .contains(responseOutputTextAnnotationAdded)
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseOutputTextAnnotationAddedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseOutputTextAnnotationAdded(
                BetaResponseOutputTextAnnotationAddedEvent.builder()
                    .annotation(JsonValue.from(mapOf<String, Any>()))
                    .annotationIndex(0L)
                    .contentIndex(0L)
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseOutputTextAnnotationAddedEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseQueued() {
        val responseQueued =
            BetaResponseQueuedEvent.builder()
                .response(
                    BetaResponse.builder()
                        .id("id")
                        .createdAt(0.0)
                        .error(
                            BetaResponseError.builder()
                                .code(BetaResponseError.Code.SERVER_ERROR)
                                .message("message")
                                .build()
                        )
                        .incompleteDetails(
                            BetaResponse.IncompleteDetails.builder()
                                .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                .build()
                        )
                        .instructions("string")
                        .metadata(
                            BetaResponse.Metadata.builder()
                                .putAdditionalProperty("foo", JsonValue.from("string"))
                                .build()
                        )
                        .model(BetaResponse.Model.GPT_5_1)
                        .addOutput(
                            BetaResponseOutputMessage.builder()
                                .id("id")
                                .addContent(
                                    BetaResponseOutputText.builder()
                                        .addAnnotation(
                                            BetaResponseOutputText.Annotation.FileCitation.builder()
                                                .fileId("file_id")
                                                .filename("filename")
                                                .index(0L)
                                                .build()
                                        )
                                        .text("text")
                                        .addLogprob(
                                            BetaResponseOutputText.Logprob.builder()
                                                .token("token")
                                                .addByte(0L)
                                                .logprob(0.0)
                                                .addTopLogprob(
                                                    BetaResponseOutputText.Logprob.TopLogprob
                                                        .builder()
                                                        .token("token")
                                                        .addByte(0L)
                                                        .logprob(0.0)
                                                        .build()
                                                )
                                                .build()
                                        )
                                        .build()
                                )
                                .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                .agent(
                                    BetaResponseOutputMessage.Agent.builder()
                                        .agentName("agent_name")
                                        .build()
                                )
                                .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                .build()
                        )
                        .parallelToolCalls(true)
                        .temperature(1.0)
                        .toolChoice(BetaToolChoiceOptions.NONE)
                        .addTool(
                            BetaFunctionTool.builder()
                                .name("name")
                                .parameters(
                                    BetaFunctionTool.Parameters.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .strict(true)
                                .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                .deferLoading(true)
                                .description("description")
                                .outputSchema(
                                    BetaFunctionTool.OutputSchema.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("bar"))
                                        .build()
                                )
                                .build()
                        )
                        .topP(1.0)
                        .background(true)
                        .completedAt(0.0)
                        .conversation(BetaResponse.Conversation.builder().id("id").build())
                        .maxOutputTokens(0L)
                        .maxToolCalls(0L)
                        .moderation(
                            BetaResponse.Moderation.builder()
                                .input(
                                    BetaResponse.Moderation.Input.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Input.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .output(
                                    BetaResponse.Moderation.Output.ModerationResult.builder()
                                        .categories(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .Categories
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(true))
                                                .build()
                                        )
                                        .categoryAppliedInputTypes(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryAppliedInputTypes
                                                .builder()
                                                .putAdditionalProperty(
                                                    "foo",
                                                    JsonValue.from(listOf("text")),
                                                )
                                                .build()
                                        )
                                        .categoryScores(
                                            BetaResponse.Moderation.Output.ModerationResult
                                                .CategoryScores
                                                .builder()
                                                .putAdditionalProperty("foo", JsonValue.from(0))
                                                .build()
                                        )
                                        .flagged(true)
                                        .model("model")
                                        .build()
                                )
                                .build()
                        )
                        .previousResponseId("previous_response_id")
                        .prompt(
                            BetaResponsePrompt.builder()
                                .id("id")
                                .variables(
                                    BetaResponsePrompt.Variables.builder()
                                        .putAdditionalProperty("foo", JsonValue.from("string"))
                                        .build()
                                )
                                .version("version")
                                .build()
                        )
                        .promptCacheKey("prompt-cache-key-1234")
                        .promptCacheOptions(
                            BetaResponse.PromptCacheOptions.builder()
                                .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                .build()
                        )
                        .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                        .reasoning(
                            BetaResponse.Reasoning.builder()
                                .context(BetaResponse.Reasoning.Context.AUTO)
                                .effort(BetaResponse.Reasoning.Effort.NONE)
                                .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                .summary(BetaResponse.Reasoning.Summary.AUTO)
                                .build()
                        )
                        .safetyIdentifier("safety-identifier-1234")
                        .serviceTier(BetaResponse.ServiceTier.AUTO)
                        .status(BetaResponseStatus.COMPLETED)
                        .text(
                            BetaResponseTextConfig.builder()
                                .formatText()
                                .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                .build()
                        )
                        .topLogprobs(0L)
                        .truncation(BetaResponse.Truncation.AUTO)
                        .usage(
                            BetaResponseUsage.builder()
                                .inputTokens(0L)
                                .inputTokensDetails(
                                    BetaResponseUsage.InputTokensDetails.builder()
                                        .cacheWriteTokens(0L)
                                        .cachedTokens(0L)
                                        .build()
                                )
                                .outputTokens(0L)
                                .outputTokensDetails(
                                    BetaResponseUsage.OutputTokensDetails.builder()
                                        .reasoningTokens(0L)
                                        .build()
                                )
                                .totalTokens(0L)
                                .build()
                        )
                        .user("user-1234")
                        .build()
                )
                .sequenceNumber(0L)
                .agent(BetaResponseQueuedEvent.Agent.builder().agentName("agent_name").build())
                .build()

        val betaResponseStreamEvent = BetaResponseStreamEvent.ofResponseQueued(responseQueued)

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).contains(responseQueued)
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseQueuedRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseQueued(
                BetaResponseQueuedEvent.builder()
                    .response(
                        BetaResponse.builder()
                            .id("id")
                            .createdAt(0.0)
                            .error(
                                BetaResponseError.builder()
                                    .code(BetaResponseError.Code.SERVER_ERROR)
                                    .message("message")
                                    .build()
                            )
                            .incompleteDetails(
                                BetaResponse.IncompleteDetails.builder()
                                    .reason(BetaResponse.IncompleteDetails.Reason.MAX_OUTPUT_TOKENS)
                                    .build()
                            )
                            .instructions("string")
                            .metadata(
                                BetaResponse.Metadata.builder()
                                    .putAdditionalProperty("foo", JsonValue.from("string"))
                                    .build()
                            )
                            .model(BetaResponse.Model.GPT_5_1)
                            .addOutput(
                                BetaResponseOutputMessage.builder()
                                    .id("id")
                                    .addContent(
                                        BetaResponseOutputText.builder()
                                            .addAnnotation(
                                                BetaResponseOutputText.Annotation.FileCitation
                                                    .builder()
                                                    .fileId("file_id")
                                                    .filename("filename")
                                                    .index(0L)
                                                    .build()
                                            )
                                            .text("text")
                                            .addLogprob(
                                                BetaResponseOutputText.Logprob.builder()
                                                    .token("token")
                                                    .addByte(0L)
                                                    .logprob(0.0)
                                                    .addTopLogprob(
                                                        BetaResponseOutputText.Logprob.TopLogprob
                                                            .builder()
                                                            .token("token")
                                                            .addByte(0L)
                                                            .logprob(0.0)
                                                            .build()
                                                    )
                                                    .build()
                                            )
                                            .build()
                                    )
                                    .status(BetaResponseOutputMessage.Status.IN_PROGRESS)
                                    .agent(
                                        BetaResponseOutputMessage.Agent.builder()
                                            .agentName("agent_name")
                                            .build()
                                    )
                                    .phase(BetaResponseOutputMessage.Phase.COMMENTARY)
                                    .build()
                            )
                            .parallelToolCalls(true)
                            .temperature(1.0)
                            .toolChoice(BetaToolChoiceOptions.NONE)
                            .addTool(
                                BetaFunctionTool.builder()
                                    .name("name")
                                    .parameters(
                                        BetaFunctionTool.Parameters.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .strict(true)
                                    .addAllowedCaller(BetaFunctionTool.AllowedCaller.DIRECT)
                                    .deferLoading(true)
                                    .description("description")
                                    .outputSchema(
                                        BetaFunctionTool.OutputSchema.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("bar"))
                                            .build()
                                    )
                                    .build()
                            )
                            .topP(1.0)
                            .background(true)
                            .completedAt(0.0)
                            .conversation(BetaResponse.Conversation.builder().id("id").build())
                            .maxOutputTokens(0L)
                            .maxToolCalls(0L)
                            .moderation(
                                BetaResponse.Moderation.builder()
                                    .input(
                                        BetaResponse.Moderation.Input.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Input.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .output(
                                        BetaResponse.Moderation.Output.ModerationResult.builder()
                                            .categories(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .Categories
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(true),
                                                    )
                                                    .build()
                                            )
                                            .categoryAppliedInputTypes(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryAppliedInputTypes
                                                    .builder()
                                                    .putAdditionalProperty(
                                                        "foo",
                                                        JsonValue.from(listOf("text")),
                                                    )
                                                    .build()
                                            )
                                            .categoryScores(
                                                BetaResponse.Moderation.Output.ModerationResult
                                                    .CategoryScores
                                                    .builder()
                                                    .putAdditionalProperty("foo", JsonValue.from(0))
                                                    .build()
                                            )
                                            .flagged(true)
                                            .model("model")
                                            .build()
                                    )
                                    .build()
                            )
                            .previousResponseId("previous_response_id")
                            .prompt(
                                BetaResponsePrompt.builder()
                                    .id("id")
                                    .variables(
                                        BetaResponsePrompt.Variables.builder()
                                            .putAdditionalProperty("foo", JsonValue.from("string"))
                                            .build()
                                    )
                                    .version("version")
                                    .build()
                            )
                            .promptCacheKey("prompt-cache-key-1234")
                            .promptCacheOptions(
                                BetaResponse.PromptCacheOptions.builder()
                                    .mode(BetaResponse.PromptCacheOptions.Mode.IMPLICIT)
                                    .ttl(BetaResponse.PromptCacheOptions.Ttl._30M)
                                    .build()
                            )
                            .promptCacheRetention(BetaResponse.PromptCacheRetention.IN_MEMORY)
                            .reasoning(
                                BetaResponse.Reasoning.builder()
                                    .context(BetaResponse.Reasoning.Context.AUTO)
                                    .effort(BetaResponse.Reasoning.Effort.NONE)
                                    .generateSummary(BetaResponse.Reasoning.GenerateSummary.AUTO)
                                    .mode(BetaResponse.Reasoning.Mode.STANDARD)
                                    .summary(BetaResponse.Reasoning.Summary.AUTO)
                                    .build()
                            )
                            .safetyIdentifier("safety-identifier-1234")
                            .serviceTier(BetaResponse.ServiceTier.AUTO)
                            .status(BetaResponseStatus.COMPLETED)
                            .text(
                                BetaResponseTextConfig.builder()
                                    .formatText()
                                    .verbosity(BetaResponseTextConfig.Verbosity.LOW)
                                    .build()
                            )
                            .topLogprobs(0L)
                            .truncation(BetaResponse.Truncation.AUTO)
                            .usage(
                                BetaResponseUsage.builder()
                                    .inputTokens(0L)
                                    .inputTokensDetails(
                                        BetaResponseUsage.InputTokensDetails.builder()
                                            .cacheWriteTokens(0L)
                                            .cachedTokens(0L)
                                            .build()
                                    )
                                    .outputTokens(0L)
                                    .outputTokensDetails(
                                        BetaResponseUsage.OutputTokensDetails.builder()
                                            .reasoningTokens(0L)
                                            .build()
                                    )
                                    .totalTokens(0L)
                                    .build()
                            )
                            .user("user-1234")
                            .build()
                    )
                    .sequenceNumber(0L)
                    .agent(BetaResponseQueuedEvent.Agent.builder().agentName("agent_name").build())
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCustomToolCallInputDelta() {
        val responseCustomToolCallInputDelta =
            BetaResponseCustomToolCallInputDeltaEvent.builder()
                .delta("delta")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseCustomToolCallInputDeltaEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCustomToolCallInputDelta(
                responseCustomToolCallInputDelta
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta())
            .contains(responseCustomToolCallInputDelta)
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone()).isEmpty
    }

    @Test
    fun ofResponseCustomToolCallInputDeltaRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCustomToolCallInputDelta(
                BetaResponseCustomToolCallInputDeltaEvent.builder()
                    .delta("delta")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseCustomToolCallInputDeltaEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    @Test
    fun ofResponseCustomToolCallInputDone() {
        val responseCustomToolCallInputDone =
            BetaResponseCustomToolCallInputDoneEvent.builder()
                .input("input")
                .itemId("item_id")
                .outputIndex(0L)
                .sequenceNumber(0L)
                .agent(
                    BetaResponseCustomToolCallInputDoneEvent.Agent.builder()
                        .agentName("agent_name")
                        .build()
                )
                .build()

        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCustomToolCallInputDone(
                responseCustomToolCallInputDone
            )

        assertThat(betaResponseStreamEvent.responseAudioDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseAudioTranscriptDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCodeDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseCodeInterpreterCallInterpreting()).isEmpty
        assertThat(betaResponseStreamEvent.responseCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseContentPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseCreated()).isEmpty
        assertThat(betaResponseStreamEvent.error()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFileSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseFunctionCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseIncomplete()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputItemDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryPartDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningSummaryTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseReasoningTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseRefusalDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseWebSearchCallSearching()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallGenerating()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseImageGenerationCallPartialImage()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallArgumentsDone()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpCallInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsCompleted()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsFailed()).isEmpty
        assertThat(betaResponseStreamEvent.responseMcpListToolsInProgress()).isEmpty
        assertThat(betaResponseStreamEvent.responseOutputTextAnnotationAdded()).isEmpty
        assertThat(betaResponseStreamEvent.responseQueued()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDelta()).isEmpty
        assertThat(betaResponseStreamEvent.responseCustomToolCallInputDone())
            .contains(responseCustomToolCallInputDone)
    }

    @Test
    fun ofResponseCustomToolCallInputDoneRoundtrip() {
        val jsonMapper = jsonMapper()
        val betaResponseStreamEvent =
            BetaResponseStreamEvent.ofResponseCustomToolCallInputDone(
                BetaResponseCustomToolCallInputDoneEvent.builder()
                    .input("input")
                    .itemId("item_id")
                    .outputIndex(0L)
                    .sequenceNumber(0L)
                    .agent(
                        BetaResponseCustomToolCallInputDoneEvent.Agent.builder()
                            .agentName("agent_name")
                            .build()
                    )
                    .build()
            )

        val roundtrippedBetaResponseStreamEvent =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(betaResponseStreamEvent),
                jacksonTypeRef<BetaResponseStreamEvent>(),
            )

        assertThat(roundtrippedBetaResponseStreamEvent).isEqualTo(betaResponseStreamEvent)
    }

    enum class IncompatibleJsonShapeTestCase(val value: JsonValue) {
        BOOLEAN(JsonValue.from(false)),
        STRING(JsonValue.from("invalid")),
        INTEGER(JsonValue.from(-1)),
        FLOAT(JsonValue.from(3.14)),
        ARRAY(JsonValue.from(listOf("invalid", "array"))),
    }

    @ParameterizedTest
    @EnumSource
    fun incompatibleJsonShapeDeserializesToUnknown(testCase: IncompatibleJsonShapeTestCase) {
        val betaResponseStreamEvent =
            jsonMapper().convertValue(testCase.value, jacksonTypeRef<BetaResponseStreamEvent>())

        val e = assertThrows<OpenAIInvalidDataException> { betaResponseStreamEvent.validate() }
        assertThat(e).hasMessageStartingWith("Unknown ")
    }
}
